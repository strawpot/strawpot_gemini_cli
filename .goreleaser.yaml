version: 2

builds:
  - dir: gemini/wrapper
    main: .
    binary: strawpot_gemini
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w

archives:
  - formats: [binary]
    name_template: "strawpot_gemini-{{ .Os }}-{{ .Arch }}"

release:
  github:
    owner: strawpot
    name: strawpot_gemini_cli
